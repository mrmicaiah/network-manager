# Bethany Dashboard Environment Variables
# Copy to .env.local and fill in values

# API URL for the Network Manager Worker
# Leave empty for same-origin (local dev), set for cross-origin production
VITE_API_URL=https://network-manager.micaiah-tasks.workers.dev
