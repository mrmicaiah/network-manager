# =============================================================================
# Bethany Network Manager — Environment Variables
# =============================================================================
# Copy this file to .dev.vars for local development:
#   cp .env.example .dev.vars
#
# For production, set secrets via wrangler CLI:
#   wrangler secret put ANTHROPIC_API_KEY
# =============================================================================

# --- Claude API ---
ANTHROPIC_API_KEY=sk-ant-...

# --- SendBlue (SMS/iMessage delivery) ---
SENDBLUE_API_KEY=
SENDBLUE_API_SECRET=
SENDBLUE_PHONE_NUMBER=+1XXXXXXXXXX

# --- Security ---
PIN_SIGNING_SECRET=          # Random 64-char hex string for HMAC token signing
INTERNAL_API_KEY=            # Shared key between Bethany worker and Network Manager

# --- D1 Database (local dev only — production uses wrangler.toml binding) ---
# database_id is set in wrangler.toml after running: npm run db:create
